_site/
.jekyll-cache/
.jekyll-metadata
node_modules/
vendor/
.sass-cache/
.idea/

# Test suite (not published until source repos are public)
__tests/

# Local test infrastructure
docker-compose.test.yml
.env
# Anchored to the root: the local test helpers live in /scripts, while
# .github/scripts holds build tooling that has to be committed.
/scripts/
/test-results.txt

# Generated at build time from package.json
_data/version.yml
